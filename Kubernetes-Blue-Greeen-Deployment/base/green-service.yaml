apiVersion: v1
kind: Service
metadata:
  name: app-green-svc
  namespace: blue-green-demo
spec:
  selector:
    app: myapp
    version: green  # This targets only green pods
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
